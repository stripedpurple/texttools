{
  "name": "texttools",
  "version": "1.0.0",
  "description": "vue base text munipulation tools",
  "author": "Austin Barrett",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "build:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt build",
    "generate:gh-pages": "DEPLOY_ENV=GH_PAGES nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "lodash": "^4.17.19",
    "nuxt": "^2.13.0",
    "nuxt-buefy": "^0.3.2",
    "voca": "^1.4.0",
    "vue-clipboard2": "^0.3.0"
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
